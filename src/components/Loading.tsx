import React from 'react';
import './Loading.css';

type Props = {};

const Loading = (props: Props) => {
  return (
    <div className="loading-page">
      <h2>Loading...</h2>
    </div>
  );
};

export default Loading;
